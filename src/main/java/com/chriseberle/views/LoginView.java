package com.chriseberle.views;

import com.chriseberle.utils.StageManager;

import javafx.stage.Stage;

public class LoginView {
    /**
     * Set the default application settings for the JavaFX window.
     * @param stage the primary stage
     */
    public static void setDefaultViewettings(Stage stage) {
        StageManager.setDefaultApplicationSettings(stage);
    }

}
